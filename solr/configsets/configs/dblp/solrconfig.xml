<?xml version="1.0" encoding="UTF-8" ?>
<config>
  <luceneMatchVersion>7.7.0</luceneMatchVersion>

  <updateHandler>
    <updateLog>
      <str name="dir">${solr.data.dir:}</str>
    </updateLog>
    <autoCommit>
      <maxTime>${solr.autoCommit.maxTime:15000}</maxTime>
      <openSearcher>false</openSearcher>
    </autoCommit>
  </updateHandler>

  <updateProcessor class="solr.ParseDateFieldUpdateProcessorFactory" name="parse-date">
    <arr name="format">
      <str>yyyy-MM-dd</str>
      <str>yyyy</str>
    </arr>
  </updateProcessor>

  <updateRequestProcessorChain name="defaultWithParseDate" default="true" processor="parse-date">
    <!-- needed for title, since it contains HTML markup -->
    <processor name="strip-html" class="solr.HTMLStripFieldUpdateProcessorFactory">
      <str name="typeClass">solr.StrField</str>
    </processor>
    <!-- when this is executed before HTML stripping, the HTMLMarkup is preserved -->
    <!--TODO can possible abuse this to keep HTML around, i.e. copy to something
        of type textfield (cannot be solr.StrField) -->
    <processor class="solr.CloneFieldUpdateProcessorFactory" name="clone-suggest">
      <arr name="source">
        <str>title</str>
        <str>year</str>
        <str>author</str>
      </arr>
      <str name="dest">suggest</str>
    </processor>
    <processor class="solr.ConcatFieldUpdateProcessorFactory">
      <str name="delimiter"> ; </str>
      <lst name="exclude">
        <str name="fieldName">url</str>
        <str name="fieldName">pub_type_str</str>
        <str name="fieldName">publtype</str>
        <str name="fieldName">address</str>
        <str name="fieldName">booktitle</str>
        <str name="fieldName">chapter</str>
        <str name="fieldName">journal</str>
        <str name="fieldName">month</str>
        <str name="fieldName">number</str>
        <str name="fieldName">title</str>
        <str name="fieldName">year</str>
        <str name="fieldName">mdate</str>
        <str name="fieldName">cdate</str>
      </lst>
    </processor>
    <!--<processor class="solr.FirstFieldValueUpdateProcessorFactory">-->
      <!--<str name="fieldName">primary_author</str>-->
    <!--</processor>-->
    <processor class="solr.LogUpdateProcessorFactory"/>
    <processor class="solr.DistributedUpdateProcessorFactory"/>
    <processor class="solr.RunUpdateProcessorFactory"/>
  </updateRequestProcessorChain>

  <dataDir>${solr.data.dir:}</dataDir>

  <query>
    <maxBooleanClauses>1024</maxBooleanClauses>
    <filterCache class="solr.FastLRUCache"  size="512" initialSize="512" autowarmCount="0"/>
    <queryResultCache class="solr.LRUCache" size="512" initialSize="512" autowarmCount="0"/>
    <documentCache class="solr.LRUCache"    size="512" initialSize="512" autowarmCount="0"/>
    <!-- custom cache currently used by block join -->
    <cache name="perSegFilter" class="solr.search.LRUCache" size="10" initialSize="0" autowarmCount="10"
           regenerator="solr.NoOpRegenerator"/>
    <enableLazyFieldLoading>true</enableLazyFieldLoading>
    <queryResultWindowSize>20</queryResultWindowSize>
    <queryResultMaxDocsCached>200</queryResultMaxDocsCached>

    <listener event="newSearcher" class="solr.QuerySenderListener">
      <arr name="queries"/>
    </listener>
    <listener event="firstSearcher" class="solr.QuerySenderListener">
      <arr name="queries"/>
    </listener>
    <useColdSearcher>false</useColdSearcher>
  </query>

  <requestDispatcher>
    <httpCaching never304="true"/>
  </requestDispatcher>

  <requestHandler name="/select" class="solr.SearchHandler">
    <lst name="defaults">
      <str name="echoParams">explicit</str>
      <int name="rows">10</int>
    </lst>
  </requestHandler>

  <requestHandler name="/query" class="solr.SearchHandler">
    <lst name="defaults">
      <str name="echoParams">explicit</str>
      <str name="wt">json</str>
      <str name="indent">true</str>
    </lst>
  </requestHandler>


  <initParams path="/update/**,/query,/select,/tvrh,/elevate,/spell">
    <lst name="defaults">
      <str name="df">_text_</str>
    </lst>
  </initParams>

  <queryResponseWriter name="xslt" class="solr.XSLTResponseWriter">
    <int name="xsltCacheLifetimeSeconds">5</int>
  </queryResponseWriter>

  <requestHandler class="solr.SearchHandler" name="/suggest" useParams="suggest">
    <lst name="defaults">
      <str name="fl">suggest</str>
      <str name="qf">suggest_lower^10 suggest_ngram</str>
    </lst>
  </requestHandler>

</config>
